{
  "name": "snowtab",
  "version": "1.0.0",
  "private": true,
  "description": "A jolly homepage with quick links, todo list, Pomodoro timer, Christmas countdown, and illustrated wallpapers.",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir -p dist && cp -R index.html favicon.svg src wallpapers dist/"
  },
  "devDependencies": {},
  "dependencies": {}
}
