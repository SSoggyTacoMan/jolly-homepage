{
  "name": "snowtab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "start": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir -p dist && cp -R index.html favicon.svg src wallpapers package.json README.md dist/"
  },
  "dependencies": {},
  "devDependencies": {}
}
